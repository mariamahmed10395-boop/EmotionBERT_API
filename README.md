# EmotionBERT_API
A secure, high-performance REST API built with FastAPI that integrates a fine-tuned Transformer model for text classification. Features include user registration, JWT-based authentication, email verification via UUID, and an AI-powered prediction endpoint.
